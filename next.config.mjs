/** @type {import('next').NextConfig} */
const nextConfig = {
    poweredByHeader: false,
    generateEtags: false,
    reactStrictMode: false,
    output: "standalone",
    trailingSlash: false,
    env: {
  },
}

export default nextConfig
